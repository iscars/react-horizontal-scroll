import React from "react";
import ReactDOM from "react-dom";
import { Slider } from "./";

ReactDOM.render(
  <React.StrictMode>
    <Slider>1</Slider>
  </React.StrictMode>,
  document.getElementById('root'),
);
